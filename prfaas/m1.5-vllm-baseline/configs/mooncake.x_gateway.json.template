{
    "local_hostname": "${X_GATEWAY_PUBLIC_IP}",
    "metadata_server": "etcd://${X_GATEWAY_PUBLIC_IP}:${ETCD_PORT}",
    "protocol": "tcp",
    "device_name": "",
    "master_server_address": "${X_GATEWAY_PUBLIC_IP}:${MOONCAKE_MASTER_PORT}"
}
