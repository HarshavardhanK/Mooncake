# Front-door OpenAI HTTP for the Config-H decoder; consumed by the proxy.
apiVersion: v1
kind: Service
metadata:
  name: decoder-h-api
  namespace: prfaas-stageb
  labels:
    prfaas.experiment/stage: "B"
    prfaas.config: "H"
    app.kubernetes.io/part-of: prfaas
spec:
  type: ClusterIP
  selector:
    app: decoder-h
  ports:
    - name: http
      port: 8020
      targetPort: api
      protocol: TCP
